{
  "name": "voai-ux-ui",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@10.8.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "test:e2e": "turbo test:e2e",
    "test:e2e:variant": "turbo test:e2e:variant",
    "storybook": "turbo storybook",
    "build-storybook": "turbo build-storybook",
    "chromatic:usage": "node scripts/chromatic-usage.js",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@playwright/test": "^1.40.0",
    "@voai/eslint-config": "file:./packages/eslint-config",
    "@voai/tsconfig": "file:./packages/tsconfig",
    "turbo": "^2.0.0"
  },
  "overrides": {
    "@babel/core": "^7.25.0"
  },
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}